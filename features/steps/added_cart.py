from behave import given, when, then
from selenium.webdriver.common.by import By
from time import sleep


#@then('Verify {expected}')
#def cart_verification(context, expected):
    #actual = (By.CSS_SELECTOR, 'span.a-size-medium-plus.a-color-base.sw-atc-text.a-text-bold')
   # assert expected == actual, f'Error, Actual text {actual} does not match {expected}'
